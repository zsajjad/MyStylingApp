/*
 * StyleScreenScreen Messages
 *
 * This contains all the text for the StyleScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.screens.StyleScreen';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StyleScreen screen!',
  },
});
