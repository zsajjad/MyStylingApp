/*
 * ModelListItem Messages
 *
 * This contains all the text for the ModelListItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ModelListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModelListItem component!',
  },
});
